<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit an Answer</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('topbar') %>
    
    <div class="container mt-5">
        <form action="/answers/<%= answer._id %>/edit" method="POST" class="mt-4">
            <div class="row justify-content-center">
                <div class="col-6 mb-3">
                    <h2><label for="title" class="form-label"><%= question.title %></label></h2>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-6 mb-3">
                    <label for="1n-body" class="form-label"><%= question.body %></label>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-6 mb-3">
                    <textarea type="text" class="form-control" id="body" rows="10" name="body"><%= answer.body %></textarea>
                </div>
            </div>

            <div class="row justify-content-center">
                <div class="text-center mt-3">
                    <button type="submit" class="btn btn-primary w-10">Edit</button>
                    <a href="/answers/list" class="btn btn-outline-danger">Cancel</a>
                </div>
            </div>        
        </form>
    </div>
</body>
</html>