<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submit a Question</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('topbar') %>
    
    <div class="container mt-5">
        <form action="/questions/<%= question._id %>/answers/new" method="POST" class="mt-4">
            <!-- Question Title -->
            <div class="row justify-content-center">
                <div class="col-6 mb-3">
                    <div class="question-title">
                        <h2><label for="title" class="form-label"><%= question.title %></label></h2>
                    </div>  
                </div>
            </div>

            <!-- Question Body -->
            <div class="row justify-content-center">
                <div class="col-6 mb-3">
                    <label for="qn-body" class="form-label"><%= question.body %></label>
                </div>
            </div>

            <!-- Answer Body -->
            <div class="row justify-content-center">
                <div class="col-6 mb-3">
                    <textarea class="form-control" id="body" name="body" rows="10" required placeholder="Provide your answer here"></textarea>
                </div>
            </div>

            <!-- Submit Button -->
            <div class="row justify-content-center">
                <div class="text-center mt-3">
                    <button type="submit" class="btn btn-primary w-10">Submit</button>
                    <a href="/questions/<%= question._id %>" class="btn btn-outline-danger">Cancel</a>
                </div>
            </div>        
        </form>
    </div>
</body>
</html>